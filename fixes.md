* pages/main_page.py  lines: 49-54 
  - Нужно исправить: здесь и далее по проекту: все прямые обращения к driver и WebDriverWait необходимо вынести в base, там описать методы взаимодействия с ними и оттуда за счет наследования классов страниц от base переиспользовать
  
* pages/main_page.py lines: 54-57 

  - Нужно исправить: здесь и далее: все локаторы хранятся отдельно от методов, а тут только переменные, в которые они были записаны. Даже этот темплейт
  
* test/test_order.py lines: 23-26

  - Нужно исправить: условий в тестах быть не может, тест всегда однозначен. Если это не вписывается в параметризацию, значит ее быть не должно и это просто несколько отдельных тестовых методов

* test/test_order.py lines: 42-45 
  - Нужно исправить: условий в тестах быть не может, тест всегда однозначен. Если это не вписывается в параметризацию, значит ее быть не должно и это просто несколько отдельных тестовых методов

* test/test_order.py 52-55 
  - Нужно исправить: здесь и далее: прямого обращения к driver, вызова его property, execute_script на нем и тд из тестов быть не должно. Для этого в base пишутся соответствующие методы и тут вызываются на объектах конкретных страниц
  
* test/test_order.py 60-63 
  - Нужно исправить: здесь и далее: прямого обращения к WebDriverWait, вызова его property, execute_script на нем и тд из тестов быть не должно. Для этого в base пишутся соответствующие методы и тут вызываются на объектах конкретных страниц
  
* tests/test_order.py
  - Нужно исправить: копипаста модуля
